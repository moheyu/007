interface:
  display_name: "007"
  short_description: "Repo-specific patterns and workflows for 007"
  default_prompt: "Use the 007 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true